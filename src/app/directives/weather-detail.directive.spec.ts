import { WeatherDetailDirective } from './weather-detail.directive';

describe('WeatherDetailDirective', () => {
  it('should create an instance', () => {
    const directive = new WeatherDetailDirective();
    expect(directive).toBeTruthy();
  });
});
